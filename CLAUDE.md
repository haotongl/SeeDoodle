# CLAUDE.md

The guidance for this repo lives in one file, [`AGENTS.md`](AGENTS.md), so that every agent and every
human reads the same thing. Claude Code picks it up through the import below.

@AGENTS.md
